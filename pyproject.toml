[project]
name = "f1-fantasy-optimiser"
version = "0.1.0"
description = "ML pipeline to predict F1 fantasy points and optimise team selection"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "pyarrow>=14.0",
    "pulp>=2.7",
]

[project.optional-dependencies]
# minimal set the Streamlit dashboard (web service) needs at runtime
dashboard = [
    "plotly>=5.0",
    "streamlit>=1.32",
]
# data ingestion, model training, and report rendering (also needs the dashboard deps)
pipeline = [
    "f1-fantasy-optimiser[dashboard]",
    "fastf1>=3.3",
    "scikit-learn>=1.3",
    "scipy>=1.11",
    "joblib>=1.3",
    "xgboost>=2.0",
    "matplotlib>=3.7",
    "pandera>=0.19",
    "typer",
    "playwright>=1.40",
]
dev = [
    "pytest>=7.0",
    "ruff>=0.4",
    "ipykernel>=6.0",
    "notebook>=7.0",
    "seaborn>=0.13",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.ruff]
line-length = 100
